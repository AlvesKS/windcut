% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_functions.R
\name{multivariable_condition_summary_functions}
\alias{multivariable_condition_summary_functions}
\alias{count_when}
\alias{proportion_when}
\alias{sum_when}
\alias{mean_when}
\alias{max_when}
\alias{min_when}
\alias{max_consecutive_when}
\alias{spell_count_when}
\alias{mean_spell_duration_when}
\alias{max_spell_duration_when}
\title{Multivariable condition summary functions}
\usage{
count_when(condition, na.rm = TRUE)

proportion_when(condition, na.rm = TRUE)

sum_when(variable, condition, na.rm = TRUE)

mean_when(variable, condition, na.rm = TRUE)

max_when(variable, condition, na.rm = TRUE)

min_when(variable, condition, na.rm = TRUE)

max_consecutive_when(condition, na.rm = TRUE)

spell_count_when(condition, na.rm = TRUE)

mean_spell_duration_when(condition, na.rm = TRUE)

max_spell_duration_when(condition, na.rm = TRUE)
}
\arguments{
\item{condition}{An expression evaluated in the window data frame.}

\item{na.rm}{If \code{TRUE}, missing values are ignored. For proportion functions,
the denominator is the number of non-missing observations.}

\item{variable}{A numeric column evaluated in the window data frame.}
}
\value{
A function marked as a windcut condition summary. It receives a data
frame and returns one numeric value.
}
\description{
These functions create summaries from expressions evaluated in a weather
window data frame. They are used inside the special \code{.conditions} entry of
\code{statistics}.
}
\examples{
weather <- data.frame(temp = c(17, 20, 24), rh = c(88, 92, 95), rain = c(0, 1, 0))
count_when(temp >= 18 & temp <= 26 & rh >= 90)(weather)

statistics <- list(
  temp = list(mean = "mean"),
  .conditions = list(
    favorable_hours = count_when(temp >= 18 & temp <= 26 & rh >= 90)
  )
)

}
