% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_weather_daily.R
\name{aggregate_weather_daily}
\alias{aggregate_weather_daily}
\title{Aggregate Weather Observations to Daily Data}
\usage{
aggregate_weather_daily(
  weather,
  id_col = NULL,
  time_col = "time",
  date_col = "date",
  weather_cols = NULL,
  temp_col = "temp",
  rh_col = "rh",
  rain_col = "rain",
  leaf_wetness_col = "leaf_wetness",
  statistics = list(temp = "mean", rh = "mean", rain = "sum", leaf_wetness = "sum"),
  keep_time = TRUE,
  name_prefix = "daily"
)
}
\arguments{
\item{weather}{A weather data frame.}

\item{id_col}{Optional site, location, or series identifier column. If
supplied, aggregation is done separately within each ID.}

\item{time_col}{Name of the timestamp column. Use \code{NULL} when the input data
already have a day/date column and no separate timestamp column is needed.}

\item{date_col}{Name of the day/date column. If this column already exists in
\code{weather}, it is used for daily grouping. If it does not exist,
\code{aggregate_weather_daily()} derives it from \code{time_col} and creates it in
the output.}

\item{weather_cols}{Character vector of weather columns to aggregate. If
named, the names are used in the output column names.}

\item{temp_col}{Name of the temperature column.}

\item{rh_col}{Name of the relative humidity column.}

\item{rain_col}{Name of the rainfall column.}

\item{leaf_wetness_col}{Name of the leaf wetness column.}

\item{statistics}{Daily statistics to compute. Use a character vector to
apply the same statistics to all \code{weather_cols}, such as
\code{c("mean", "max")}. Use a named list to choose statistics by variable or
to provide custom named functions. Use \code{.conditions} for multivariable
condition summaries.}

\item{keep_time}{If \code{TRUE}, include a daily \code{POSIXct} time column at midnight.
This requires either \code{time_col} or an existing \code{date_col}.}

\item{name_prefix}{Prefix used in aggregated weather columns. The default
creates names such as \code{daily_mean_temp} and \code{daily_sum_rain}.}
}
\value{
A daily data frame sorted by ID and date.
}
\description{
Converts sub-daily weather observations to one row per day, optionally within
each site or location. The same \code{statistics} style used by the window
workflow is supported here: use a character vector to apply the same
statistics to every variable, or use a named list to choose different
statistics for different variables. The special \code{.conditions} entry can be
used for multivariable summaries within each daily group.
}
\examples{
hourly <- simulate_weather_series(days = 5, n_series = 2, id_col = "site_id", seed = 1)

aggregate_weather_daily(
  hourly,
  id_col = "site_id",
  statistics = list(temp = c("mean", "max"), rh = "mean", rain = "sum")
)

}
